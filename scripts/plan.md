# Plan
1. Implement App Router based admin login and RBAC (cookie-based for now, server-side RBAC later).
2. Create Supabase-backed API routes for products/orders with full CRUD access controlled by admin.
3. Build App Router UI pages for Products, Orders, and POS with realtime updates using Supabase real-time.
4. Ensure .env.local usage and seed script to create initial data.
5. Verify no routing conflicts and all pages render without errors.
